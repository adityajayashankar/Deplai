resource "aws_eip" "app" {
  count    = var.enabled && var.enable_eip ? 1 : 0
  domain   = "vpc"
  instance = module.ec2[0].id
  tags     = merge(var.common_tags, { Name = "${var.project_name}-${var.environment}-eip" })
}
